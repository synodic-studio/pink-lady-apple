# Standard Appfile.
# Substitute APP_IDENTIFIER with the real bundle ID when stamping.
# Team ID is shared across your team (<Team Name>).
# Apple ID is intentionally omitted — we authenticate via ASC API key,
# which doesn't need it.

app_identifier("APP_IDENTIFIER")
team_id("<TEAM_ID>")
